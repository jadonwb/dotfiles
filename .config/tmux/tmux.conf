set-option -g default-shell /usr/bin/zsh

#True color support
set -ga terminal-overrides ",screen-256color*:Tc"
set -g default-terminal "screen-256color"
set -s escape-time 0
set -gq allow-passthrough on

# Change prefix from Ctrl+b to Ctrl+a
unbind C-b
set-option -g prefix C-a
bind-key C-a send-prefix

# Reload config
bind r source-file ~/.config/tmux/tmux.conf \; refresh-client \; display-message "Config reloaded!"

# misc
set -g base-index 1
set -g pane-base-index 1
set -g renumber-windows on

# resizing
bind -r h resize-pane -L 5
bind -r j resize-pane -D 5
bind -r k resize-pane -U 5
bind -r l resize-pane -R 5

# Split panes using | and -
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"
unbind '"'
unbind %

# Mouse support
set -g mouse on

# Copy mode
unbind v
bind v copy-mode

# vim shell navigations and selects in tmux panes
set-window-option -g mode-keys vi
bind-key -T copy-mode-vi 'v' send -X begin-selection
bind-key -T copy-mode-vi 'y' send -X copy-pipe-and-cancel

bind '/' if-shell -F '#{==:#{session_name},quick-terminal}' \
    'detach-client' \
    'if-shell "tmux has-session -t quick-terminal 2>/dev/null" \
        "display-popup -d \"#{pane_current_path}\" -w 130 -h 40 -E \"tmux attach-session -t quick-terminal\"" \
        "display-popup -d \"#pane_current_path}\" -w 130 -h 40 -E \"tmux new-session -s quick-terminal\""'

bind 'f' display-popup \
	-d "#{pane_current_path}" \
	-w 80 -h 40 \
	-E "~/.local/bin/tmux-sessionizer #{pane_current_path}"

# CUSTOMIZE: Add your own project hotkeys
# bind-key -r G run-shell "~/bin/tmux-sessionizer ~/projects/my-main-project"
# bind-key -r C run-shell "~/bin/tmux-sessionizer ~/work/current-client"
# bind-key -r P run-shell "~/bin/tmux-sessionizer ~/personal/side-project"

# Plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'rose-pine/tmux'

set -g @rose_pine_variant 'main'

### Rose-pine stuff ################################################################################################################

set -g @rose_pine_host 'on' # Enables hostname in the status bar
set -g @rose_pine_date_time '' # It accepts the date UNIX command format (man date for info)
set -g @rose_pine_user 'on' # Turn on the username component in the statusbar
set -g @rose_pine_directory 'on' # Turn on the current folder component in the status bar
set -g @rose_pine_bar_bg_disable 'on' # Disables background color, for transparent terminal emulators
# If @rose_pine_bar_bg_disable is set to 'on', uses the provided value to set the background color
# It can be any of the on tmux (named colors, 256-color set, `default` or hex colors)
# See more on http://man.openbsd.org/OpenBSD-current/man1/tmux.1#STYLES
set -g @rose_pine_bar_bg_disabled_color_option 'default'

# set -g @rose_pine_only_windows 'on' # Leaves only the window module, for max focus and space
# set -g @rose_pine_disable_active_window_menu 'on' # Disables the menu that shows the active window on the left

# set -g @rose_pine_default_window_behavior 'on' # Forces tmux default window list behaviour
# set -g @rose_pine_show_current_program 'on' # Forces tmux to show the current running program as window name
# set -g @rose_pine_show_pane_directory 'on' # Forces tmux to show the current directory as window name
# Previously set -g @rose_pine_window_tabs_enabled

# Example values for these can be:
# set -g @rose_pine_left_separator ' > ' # The strings to use as separators are 1-space padded
# set -g @rose_pine_right_separator ' < ' # Accepts both normal chars & nerdfont icons
# set -g @rose_pine_field_separator ' | ' # Again, 1-space padding, it updates with prefix + I
# set -g @rose_pine_window_separator ' - ' # Replaces the default `:` between the window number and name

# These are not padded
set -g @rose_pine_session_icon '' # Changes the default icon to the left of the session name
set -g @rose_pine_current_window_icon '' # Changes the default icon to the left of the active window name
set -g @rose_pine_folder_icon '' # Changes the default icon to the left of the current directory folder
set -g @rose_pine_username_icon '' # Changes the default icon to the right of the hostname
set -g @rose_pine_hostname_icon '󰒋' # Changes the default icon to the right of the hostname
set -g @rose_pine_date_time_icon '󰃰' # Changes the default icon to the right of the date module
set -g @rose_pine_window_status_separator "  " # Changes the default icon that appears between window names

# Very beta and specific opt-in settings, tested on v3.2a, look at issue #10
# set -g @rose_pine_prioritize_windows 'on' # Disables the right side functionality in a certain window count / terminal width
# set -g @rose_pine_width_to_hide '80' # Specify a terminal width to toggle off most of the right side functionality
# set -g @rose_pine_window_count '5' # Specify a number of windows, if there are more than the number, do the same as width_to_hide

####################################################################################################################################
# Move status bar to top
set -g status-position top

# Disable pane border status (removes the bottom bar with pane info)
set -g pane-border-status off
#####################################################################################################################################

set -g @continuum-restore 'on'

set -g @yank_selection 'both'
set -g @override_copy_command 'wl-copy'

# Initialize TMUX plugin manager
run '~/.tmux/plugins/tpm/tpm'
