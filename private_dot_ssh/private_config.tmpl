{{- if eq .machine.type "work" }}
Host web1 web1.example.invalid
        Port 2222
{{- end}}

{{ range $host, $config := .hosts -}}
{{- if ne $host $.chezmoi.hostname }}
Host {{ $host }}
    User {{ $config.user }}

{{- end }}
{{- end }}

AddKeysToAgent yes
