# Machine-local host overrides, not tracked by chezmoi
Include ~/.ssh/config.d/*.conf

{{ range $host, $config := .hosts -}}
{{- if ne $host $.chezmoi.hostname }}
Host {{ $host }}
    User {{ $config.user }}

{{- end }}
{{- end }}

AddKeysToAgent yes
