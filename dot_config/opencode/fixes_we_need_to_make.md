
- integrate execute into the survey propose plan loop as the next step, so that
  each change is smaller and more incremental, but still plan out working self
  contained changes. refer to as 'implementation cycle'

- since we are making changes more often, we create build briefs more often, we
  should compress at the end of each SUCCESFUL implementation cycle

- ALWAYS still ask user if ready to execute edits after finalizing on the brief,
  ALWAYS

- make build brief cleaner by adding line breaks into the format and visually
  separating things better, also if possible center the content OR display the
  'find' 'replace' blocks side by side?

- still continuously update session memory BEFORE compression

- turn the current build brief into a document in .opencode/brief.md ?
  - does make it easier to keep session clean, and me in the loop
  - can pass around brief easier to agents without messing anything up in the
    future, can just tell subagent - read this file, or orchestrate reads it
    first and has it fresh in context again.
  - cleans up brief when done, but reuses same file (just empties it of content
    with : > FILE or whatever the syntax is)?

- memory review is not to be automatic, only at user request.
